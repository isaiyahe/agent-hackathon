{
  "name": "repro-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch --experimental-strip-types src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p ."
  },
  "dependencies": {
    "@openai/agents": "^0.1.0",
    "hono": "^4.6.14",
    "@hono/node-server": "^1.13.7",
    "octokit": "^4.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
